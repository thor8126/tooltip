import React, { useState } from 'react';
import './Tooltip.css';

const Tooltip = ({ position, text }) => {
  const [show, setShow] = useState(false);

  const handleHover = () => {
    setShow(true);
  };

  const handleMouseLeave = () => {
    setShow(false);
  };

  return (
    <div className="tooltip-container">
      <button onMouseEnter={handleHover} onMouseLeave={handleMouseLeave}>
        Hover Me
      </button>
      {show && <div className={`tooltip ${position}`}>{text}</div>}
    </div>
  );
};

export default Tooltip;
And here's the CSS code for the Tooltip component:

css
Copy code
.tooltip-container {
  position: relative;
}

.tooltip {
  position: absolute;
  z-index: 1;
  padding: 5px;
  background-color: #333;
  color: #fff;
  border-radius: 5px;
}

.tooltip.top {
  bottom: 100%;
  left: 50%;
  transform: translateX(-50%);
}

.tooltip.bottom {
  top: 100%;
  left: 50%;
  transform: translateX(-50%);
}

.tooltip.right {
  top: 50%;
  left: 100%;
  transform: translateY(-50%);
}

.tooltip.left {
  top: 50%;
  right: 100%;
  transform: translateY(-50%);
}
Note that this CSS code assumes that the parent element of the Tooltip component has position: relative set. You can adjust the styles as needed to fit your specific use case.

I hope this helps you create your tooltip component! Let me know if you have any questions or if there's anything else I can help you with.